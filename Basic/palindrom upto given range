limit=int(input("given a range of find palindrom numbet "))
print("palindrom number 1 to %d :"%limit)
for num in range(1,limit+1):
    temp=num;
    sum=0;
    rem=0
    while(num!=0):
        rem=num%10
        sum=sum*10+rem
        num=num//10
    if(temp==sum):
        print(temp,end=",")
